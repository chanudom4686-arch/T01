# 🔍 วิเคราะห์และคัดแยกฟีเจอร์จาก Web V.10 สู่ Web V.1

จากการวิเคราะห์โครงสร้างไฟล์และซอร์สโค้ดในโฟลเดอร์ `web2/web_V.10` ระบบเดิมมีฟีเจอร์ที่ครบถ้วนและครอบคลุมทั้งการแสดงผล การจัดการอุปกรณ์ และความปลอดภัย โดยสามารถคัดแยกกลุ่มฟีเจอร์ (Features Breakdown) ออกมาได้ดังนี้ เพื่อเตรียมนำไปพัฒนาต่อใน **Web V.1**:

---

## 🔐 1. ระบบรักษาความปลอดภัยและการยืนยันตัวตน (Security & Auth)
- **การเข้าสู่ระบบ (Login/Logout):** มีระบบ Session ป้องกันไม่ให้คนภายนอกเข้าถึงหน้าตั้งค่า หรือสั่งเปิด/ปิดรีเลย์ได้ (`api/login.php`, `api/logout.php`)
- **ซ่อนรหัสผ่านบน Cloud (GAS Integration):** มีการใช้ Google Apps Script (GAS) เก็บ `ADMIN_PASSWORD` และ `API_KEY` ไว้บน Cloud แทนการ Hardcode ในไฟล์ เพื่อป้องกันการถูกขโมยรหัสผ่าน (`config/gas_helper.php`, `config/secrets.php`)
- **การเปลี่ยนรหัสผ่าน:** มีระบบให้แอดมินเปลี่ยนรหัสผ่านได้เองจากหน้าเว็บ และซิงค์รหัสผ่านใหม่กลับไปเก็บบน GAS อัตโนมัติ (`api/change_password.php`)

---

## 📡 2. ระบบจัดการและรับส่งข้อมูลจาก IoT (Data & Device Management)
- **ระบบ Auto-Provisioning (Heartbeat):** บอร์ด ESP32 ใหม่ที่เพิ่งเปิดเครื่อง สามารถมารายงานตัวเพื่อขอรับ Config (เช่น Mode, Send Interval) อัตโนมัติ (`api/heartbeat.php`)
- **รับค่าจากเซ็นเซอร์ (Data Ingestion):** รองรับการรับข้อมูล HTTP POST จากบอร์ด นำมาจัดเก็บลงฐานข้อมูล (`api/data_update.php`)
- **จัดการสถานีเซ็นเซอร์:** แอดมินสามารถดูสถานะ Online/Offline ของบอร์ดแต่ละตัว เพิ่ม/ลบบอร์ด และเปลี่ยนโหมดบอร์ดได้จากหน้าเว็บ (`api/get_devices.php`, `api/provision.php`, `api/delete_device.php`, `views/admin_devices.php`)

---

## 📊 3. ระบบแสดงผล (Dashboards & Visualization)
ระบบเดิมมีการแยกหน้าจอ (Views) ออกตามจุดประสงค์การใช้งานอย่างชัดเจน:
- **โหมด 4 (สภาพอากาศ):** แสดงอุณหภูมิ, ความชื้น, ปริมาณน้ำฝน, ความเร็วลม (`views/dashboard_weather.php`)
- **โหมด 1 (ระดับน้ำคลอง/บ่อ):** แสดงระดับน้ำขาเข้า/ขาออก พร้อมเปอร์เซ็นต์การเปิดบานประตูน้ำ (`views/dashboard_river.php`)
- **โหมด 2 (น้ำประปา):** แสดงระดับน้ำดิบและน้ำดี (`views/dashboard_tap.php`)
- **โหมดสมดุลน้ำ:** ระบบคำนวณสูตรคณิตศาสตร์แบบเรียลไทม์ (Dynamic Calculation) นำความกว้างบานประตูมาคำนวณปริมาตรน้ำไหลเข้า/ออก (`views/dashboard_water_balance.php`)
- **กราฟและประวัติ:** ดึงข้อมูลย้อนหลังมาวาดกราฟเส้นด้วย Chart.js เลือกระยะเวลาได้ (`views/dashboard_history.php`, `api/get_history.php`)
- **Dashboard รวมอุปกรณ์:** หน้าจอเช็คสุขภาพของบอร์ดทั้งหมดว่าเชื่อมต่อล่าสุดเมื่อไหร่ (`views/dashboard_devices.php`)

---

## ⚙️ 4. ระบบตั้งค่าและคำสั่งควบคุม (Settings & Control)
- **การควบคุมรีเลย์ (Relay Control):** แอดมินสามารถสั่ง เปิด/ปิด ปั๊มน้ำหรืออุปกรณ์ที่ต่อกับรีเลย์ได้จากหน้าเว็บ โดยเว็บจะนำคำสั่งไปพักไว้รอให้ ESP32 มารับไปทำงาน (`api/set_relay.php`)
- **การตั้งค่าพารามิเตอร์ (Dynamic Settings):** มีหน้าตั้งค่าตัวแปรต่างๆ เช่น ความกว้างบานประตู, Zero Gauge, ระยะเซ็นเซอร์เมื่อปิดสนิท 0% และ 100% เพื่อให้ระบบนำไปคำนวณได้แม่นยำ (`views/setting.php`, `api/save_settings.php`)

---

## 💾 5. ระบบฐานข้อมูลและการ Export (Database & Export)
- **SQLite Database:** เปลี่ยนจากการใช้ไฟล์ `.csv` ล้วนๆ มาใช้ฐานข้อมูล SQLite ทำให้ค้นหาและดึงข้อมูลย้อนหลังได้รวดเร็วขึ้น (`config/database.php`, `config/setup_db.php`)
- **ดาวน์โหลดรายงาน (CSV Export):** มีฟีเจอร์กดดาวน์โหลดข้อมูลย้อนหลังออกมาเป็นไฟล์ Excel/CSV สำหรับนำไปทำรายงานต่อ (`api/export_csv.php`, `api/export_settings_log.php`)
- **ล้างข้อมูล:** ฟีเจอร์ลบข้อมูลเก่าทิ้งเมื่อไม่ต้องการ (`api/clear_data.php`)

---

### 🎯 สรุปแนวทางการพัฒนา Web V.1 จากฟีเจอร์ด้านบน:
เพื่อให้โปรเจกต์ `web_V.1` ออกมาสมบูรณ์แบบ เราควรจะ:
1. **(Backend)** ยกเครื่อง API เหล่านี้ทั้งหมดไปไว้ในโฟลเดอร์ `Backend_PHP` บน `iwasam.com` โดยเขียนโค้ดให้สะอาดขึ้นและตอบกลับเป็น JSON 100%
2. **(Frontend)** สร้างหน้าเว็บใหม่ทั้งหมดในโฟลเดอร์ `Frontend_Web` โดยใช้ HTML/JS ยิงคำสั่ง `fetch()` มาเรียกใช้ API ในข้อ 1
3. **(Design)** ปรับปรุงหน้าตา UI ในส่วนของ Dashboard (ข้อ 3) ให้ทันสมัย (Modern & Glassmorphism) และรวมศูนย์ข้อมูลให้ดูง่ายกว่าเดิม
